[workspace]
members = [
    "filter",
    "filter_odd",
    "filter_json",
    "filter_regex",
    "map",
    "map_double",
    "map_json",
    "map_regex",
]

# lto is need to reduce wasm binary size
[profile.release]
lto = true