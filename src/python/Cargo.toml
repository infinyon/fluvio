[package]
name = "fluvio-rust"
version = "0.1.0"
edition = "2018"
authors = ["foobar"]

[lib]
crate-type = ["cdylib"]
name = "fluvio_rust"

[build-dependencies]
flapigen = { git = "https://github.com/Dushistov/flapigen-rs" }
env_logger = "^0.7"
log = "^0.4.6"

[dependencies]
cpython = { version = "0.5", features = ["extension-module"] }
