version: 0.1.0
name: my-test-mqtt
type: mqtt
topic: my-mqtt
create_topic: false
direction: source
parameters:
  param_1: "mqtt.hsl.fi"
  header:
    - "auth: foo"
  test:
     auth: "foo"
secrets:
  foo: bar
